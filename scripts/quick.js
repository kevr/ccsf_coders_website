
/*
var TOP_OFFSET = 10;

var setQuick = function() {
  var quick = $('.quick-links');
  var nav = $('nav');
  quick.css('top', (nav.height() + TOP_OFFSET) + 'px');
}

$(this).scroll(function(event) {
  var scrollOffset = $(this).scrollTop();
  var links = $('.quick-links');
  var nav = $('nav');

  if(logo.height() > scrollOffset)
    links.css('top', (nav.height() - scrollOffset + TOP_OFFSET) + 'px');
  else
    links.css('top', TOP_OFFSET);
});

$(function() {
	setQuick();
});


*/
